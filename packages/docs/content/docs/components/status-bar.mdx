---
title: StatusBar
description: Horizontal bar displaying keybinding hints
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { RunDemo } from '@/components/ui/run-demo'
import StatusBarDemo from '@/components/demos/status-bar-demo'

The `StatusBar` component from `@gridland/ui` renders a horizontal bar that
displays keybinding hints showing key combinations and their associated actions.

<div className="my-8">
  <StatusBarDemo />
</div>

<RunDemo name="status-bar" />

## Installation

<Tabs items={['bun', 'npm', 'yarn', 'pnpm']}>
<Tab value="bun">
```bash title="Terminal"
bun add @gridland/ui
```
</Tab>
<Tab value="npm">
```bash title="Terminal"
npm install @gridland/ui
```
</Tab>
<Tab value="yarn">
```bash title="Terminal"
yarn add @gridland/ui
```
</Tab>
<Tab value="pnpm">
```bash title="Terminal"
pnpm add @gridland/ui
```
</Tab>
</Tabs>

## Usage

```tsx title="Example"
import { StatusBar } from "@gridland/ui"

<StatusBar
  items={[
    { key: "Tab", label: "switch focus" },
    { key: "q", label: "quit" },
  ]}
/>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `StatusBarItem[]` | -- | Array of keybinding hints to display |
| `extra` | `ReactNode` | `undefined` | Optional inline content rendered left of the hints (must be `<span>` or string) |

### StatusBarItem

| Field | Type | Description |
|-------|------|-------------|
| `key` | `string` | Key or key combination text |
| `label` | `string` | Description of what the key does |
